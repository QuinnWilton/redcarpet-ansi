require 'spec_helper'

describe Redcarpet::Ansi do
  it 'has a version number' do
    expect(Redcarpet::Ansi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
